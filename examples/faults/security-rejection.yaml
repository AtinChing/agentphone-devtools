name: Reject an unsigned webhook
description: Verifies that the handler rejects a request with its signature header removed.
channel: sms
agentId: agt_local
numberId: num_local
from: "+15559876543"
to: "+15551234567"
conversationState: null
contextLimit: 10
timeoutSeconds: 30
expectedOutcome: failed
turns:
  - caller: "This delivery should be rejected before it reaches agent logic."
    fault:
      omitSignature: true
    expect:
      status: 401
      retries: 0
